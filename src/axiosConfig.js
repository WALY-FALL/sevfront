/*import axios from "axios";

const api = axios.create({
  baseURL: process.env.REACT_APP_API_URL, // variable à définir
  withCredentials: true, // facultatif selon ton backend
});

export default api;*/
